spring:
  datasource:
    url: jdbc:mysql://localhost:3306/grupo16-oo2-2020
    username: root
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      dialect: org.hibernate.dialect.MySQL5Dialect
  thymeleaf:
    cache: false
